{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Kiosk window with only application-owned commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}

