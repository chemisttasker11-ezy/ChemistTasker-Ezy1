# Server-only Django API root. Never point local tests at production.
HUB_API_URL=http://127.0.0.1:8000/api
# Canonical public origin for articles, Open Graph and sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Existing Vite application (registration, recovery, legal pages).
NEXT_PUBLIC_PLATFORM_URL=https://chemisttasker.com.au
